// This page was intentionally removed from the application.
// The file remains as an inert placeholder to avoid import errors in older branches.

export default function VerifyEmailSentPlaceholder() {
  return null;
}
